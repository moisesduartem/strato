{% extends 'layouts/index.twig' %}

{% block content %}
    <h5 class="text-center mb-3">User {{user.id}}</h5>
    <table class="text-center table">
        <thead>
            <tr>
                <th>id</th>
                <th>name</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>{{user.id}}</td>
                <td>{{user.name}}</td>
            </tr>
        </tbody>
    </table>
{% endblock %}